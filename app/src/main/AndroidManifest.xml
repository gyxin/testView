<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tangcco.application22">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"></activity>
        <activity
            android:name=".AutoText"
            android:label="@string/title_activity_auto_text"></activity>
        <activity
            android:name=".AutoTextActivity"
            android:label="@string/title_activity_auto_text">

        </activity>
        <activity
            android:name=".Recyview"
            android:label="@string/title_activity_auto_text">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>